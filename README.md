# silicon

silicon 为通用基础库（C/C++），本仓库是其源码工程（分支 `leo/dev`）。

> **协作、接入、记忆与进度全部承载于 WorkBuddy 原生资料库「vergestudio/silicon」**，
> 仓库内**不再维护** `memory/`、`progress/`、代码接入细节等协作文档，避免多处不一致。

## 从这里获取全部信息（记忆 / 进度 / 代码接入）

新成员或新会话接入本工程，**先读 WorkBuddy 原生资料库「vergestudio/silicon」**，其内容为唯一权威、集中维护：

- **代码获取与仓库接入**（仓库地址 / `leo/dev` 分支 / 工蜂认证 / 克隆命令）：资料库 `memory/` 的 **repo** 页。
- **记忆**（工程稳定知识 / 约定 / 踩坑，按主题拆分）：入口为资料库 `memory/` 的 **MEMORY** 页（主题索引），
  按需读 `repo` / `build` / `verify` / `adr` / `conventions` / `collab` 页。
- **进度**（当前进度快照 / 变更日志 / 待办）：见资料库 `progress/` 的 **PROGRESS** 页。

每轮协作纪律（**轮前读记忆+进度 → 轮中更新 → 轮后同步回资料库**）以资料库 `memory/` 的 **collab**、**conventions** 页为准。

> 记忆 / 进度 / 接入说明仅存于资料库，不在本仓库内重复。
