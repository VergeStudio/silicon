// Implementation unit for silicon::network (send_status).
//
// send_status is a pure enum with no out-of-line definitions; this unit exists
// so the module build has a stable object for the partition and is intentionally
// empty.

module;

module silicon.network;

namespace silicon::network {

} // namespace silicon::network
