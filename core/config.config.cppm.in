module;

#include <cstdint>
#include <string>

#include "silicon/config/common.h"

export module silicon.config:config;

namespace silicon::config {
export inline CONFIG_API const std::string GetVersion() { return "${VERSION}"; }
export inline CONFIG_API std::uint32_t GetVersionMajor() { return ${VERSION_MAJOR}; }
export inline CONFIG_API std::uint32_t GetVersionMinor() { return ${VERSION_MINOR}; }
export inline CONFIG_API std::uint32_t GetVersionAlter() { return ${VERSION_ALTER}; }
export inline CONFIG_API std::uint64_t GetVersionBuild() { return ${VERSION_BUILD}; }
export inline CONFIG_API const std::string GetVersionBranch() { return "${GIT_BRANCH}"; }
export inline CONFIG_API const std::string GetVersionCommit() { return "${GIT_COMMIT}"; }
} // namespace silicon::config
